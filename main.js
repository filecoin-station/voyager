console.error('The Voyager program has ended. Thank you all for participating!')
console.error('We\'re going to retroactively reward all participants.')
console.error('Stay tuned!')
while (true) await new Promise(resolve => setTimeout(resolve, 10_000))

// import { runSaturnBenchmarkLoop } from './voyager.js'

// await runSaturnBenchmarkLoop()
